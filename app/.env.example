# Environment Variables - Copy to .env.local
# ============================================
# CivilLLM Configuration
# ============================================

# Database
DATABASE_URL="postgresql://user:password@localhost:5432/civilllm?schema=public"

# ============================================
# Authentication (NextAuth.js)
# ============================================

NEXTAUTH_URL="http://localhost:3000"
# IMPORTANT: Generate a strong secret for production (min 32 chars)
# You can generate one with: openssl rand -base64 32
NEXTAUTH_SECRET="your-super-secret-key-change-in-production"

# Google OAuth (optional)
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# ============================================
# AI Integration
# ============================================

# OpenAI API Key (required for chat functionality)
# Get your key at: https://platform.openai.com/api-keys
# SECURITY: Never commit real keys to version control!
OPENAI_API_KEY="your-openai-api-key"

# ============================================
# Security Configuration (optional)
# ============================================

# Rate Limiting
# Window duration in milliseconds (default: 60000 = 1 minute)
# RATE_LIMIT_WINDOW_MS=60000
# Maximum requests per window (default: 100)
# RATE_LIMIT_MAX_REQUESTS=100

# ============================================
# Production Checklist
# ============================================
# Before deploying to production, ensure:
# 1. NEXTAUTH_SECRET is a strong, unique value (32+ chars)
# 2. OPENAI_API_KEY is set to a real key
# 3. DATABASE_URL points to production database
# 4. All "your-" placeholders are replaced with real values
# 5. This .env file is NOT committed to version control
